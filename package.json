{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:deploy": "pnpm run typecheck && pnpm --filter @workspace/blickling-fieldbook run build && pnpm --filter @workspace/api-server run build",
    "start": "NODE_ENV=production pnpm --filter @workspace/api-server run start",
    "test": "pnpm -r --if-present run test",
    "check": "pnpm run typecheck && pnpm run test && node --check artifacts/blickling-fieldbook/public/service-worker.js && pnpm run build:deploy",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.5",
    "typescript": "~5.9.3"
  }
}
